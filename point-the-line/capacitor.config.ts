import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Point the Line',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
